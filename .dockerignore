# The worker image only needs scripts/status.mjs; keep the build context tiny so Railway
# uploads/builds fast and never pulls in the frontend or local secrets.
node_modules
dist
.git
.env
.env.local
.vercel
